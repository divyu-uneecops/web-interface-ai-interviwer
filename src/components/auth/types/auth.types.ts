/**
 * Auth types re-export
 *
 * Some auth components import types from `../types/auth.types`.
 * The source-of-truth interfaces live in `../interfaces/auth.interfaces`.
 */

export type {
  LoginFormValues,
  SignupFormValues,
  VerificationFormValues,
  ValidationError,
} from "../interfaces/auth.interfaces";


